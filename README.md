# Proyek Cuaca (cuaca)

Deskripsi
- Proyek sederhana menampilkan informasi cuaca (nama file: `cuaca.html`, `cuaca.js`, `style.css`).

Struktur berkas
- `cuaca.html` : Halaman utama — buka di browser.
- `cuaca.js` : Script utama (logika pengambilan/penampilan cuaca).
- `style.css` : Gaya utama untuk halaman.

Cara menjalankan
1. Buka `cuaca.html` langsung di browser, atau jalankan server lokal sederhana di direktori ini:

```fish
# jalankan server HTTP sederhana (port 8000)
python3 -m http.server 8000
# lalu buka http://localhost:8000/cuaca.html
```
Kontak
- Pembuat: (isi sesuai kebutuhan)
